const initState = {
    other: ["第一件事情", "第二件事情"],
  };
  
  const otherReducer = (state = initState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  export default otherReducer;
  